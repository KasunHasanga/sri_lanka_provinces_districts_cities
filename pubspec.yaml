name: sri_lanka_provinces_districts_cities
description: "A Flutter package to access information about Sri Lanka's provinces, districts, and cities."
version: 0.0.4
homepage: https://github.com/KasunHasanga/sri_lanka_provinces_districts_cities

environment:
  sdk: '>=3.4.1 <4.0.0'
  flutter: ">=1.17.0"

dependencies:
  flutter:
    sdk: flutter

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: 6.0.0


flutter:
  uses-material-design: true

#  dart format .
#  dart pub publish --dry-run